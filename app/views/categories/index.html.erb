<div class="bg-primary rounded-lg w-40">
  Button
</div>

<div class="flex bg-gray-200">
  <div class="flex-auto text-gray-700 text-center bg-gray-400 px-4 py-2 m-2 rounded-lg">
    <h1>Mes documents</h1>
    <div class="flex bg-gray-200">
      <% @categories.each do |category| %>
        <%= render 'shared/file_card', category: category.name %>
      <% end %>
    </div>

  </div>
  <div class="flex-auto text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">2</div>
</div>


